<Window x:Class="WpfApp8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp8"
        mc:Ignorable="d"
        Title="Вкладошки" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="Hide" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid>
                            <TextBox Text="{Binding Text, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Text="Your Prompt Here"
                Margin="5,0,5,0"
                Foreground="#FF808080"
                FontStyle="Italic"
                IsHitTestVisible="False"
                x:Name="UserMessage"
                Visibility="Hidden"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsKeyboardFocusWithin" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Visibility" TargetName="UserMessage" Value="Visible"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
               </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="21"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="Файл">
                <MenuItem Header="Открыть"/>
                <MenuItem Header="Сохранить"/>
                <Separator/>
                <MenuItem Header="Выход"/>
            </MenuItem>
            <MenuItem Header="Настройки">
                <MenuItem Header="Общие">
                    <MenuItem Header="Цвет фона окна"/>
                </MenuItem>
                <MenuItem Header="Дополнительные">
                    <MenuItem Header="Размер окна"/>
                </MenuItem>

            </MenuItem>
            <MenuItem Header="Помощь">
                <MenuItem Header="Справка"/>
                <Separator/>
                <MenuItem Header="Выход"/>
            </MenuItem>
        </Menu>
        <DockPanel Grid.Row="1">
            <TabControl>
                <TabItem Header="Вкладка 1">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <Button Grid.Row="0" Content="Нажми на меня" VerticalAlignment="Center" HorizontalAlignment="Center"
                                Padding="5 3 5 3">
                        </Button>
                        <RadioButton Grid.Row="1" Content="Выбери меня" VerticalAlignment="Center" HorizontalAlignment="Center">
                        </RadioButton>
                    </Grid>
                </TabItem>
                <TabItem Header="Вкладка 2">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBox Style="{StaticResource ResourceKey=Hide}" Grid.Row="0" Margin="10"/>
                        <CheckBox Grid.Row="1" Content="Отметь меня" Margin="10">
                            
                        </CheckBox>
                        <ComboBox Grid.Row="2" Width="199" Height="30" SelectedIndex="0" HorizontalAlignment="Left" Margin="10" VerticalAlignment="Top">
                            <ComboBoxItem Content="Элемент 1"/>
                            <ComboBoxItem Content="Элемент 2"/>
                            <ComboBoxItem Content="Элемент 3"/>
                        </ComboBox>
                    </Grid>
                </TabItem>
                <TabItem Header="Бесполезная вкладка 3">
                    <TextBlock Text="Тут просто написан текст" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Teal" FontSize="60"></TextBlock>
                </TabItem>
                <TabItem Header="Вкладка 4">
                    <StackPanel Margin="10">
                        <RadioButton Content="Переключатель 1" Margin="10 30" Foreground="Green"/>
                        <RadioButton Content="Переключатель 2" Margin="10 30" Foreground="Green"/>
                        <RadioButton Content="Переключатель 3" Margin="10 30" Foreground="Green"/>
                    </StackPanel>
                </TabItem>
            </TabControl>
        </DockPanel>
    </Grid>
</Window>
