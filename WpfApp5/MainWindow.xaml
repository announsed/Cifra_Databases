<Window x:Class="WpfApp5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp5"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="800">
    <Window.Resources>
        <Style x:Key="rectangleStyle" TargetType="Rectangle">
            <Setter Property="RadiusX" Value="26"/>
            <Setter Property="RadiusY" Value="26"/>
            <Setter Property="Fill">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#4b42f5" Offset="0.1"/>
                        <GradientStop Color="#9993fa" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="myButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Rectangle x:Name="myRectangle"
                                    Width="auto" Height="90"
                                    Style="{StaticResource rectangleStyle}" />
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                            TextElement.Foreground="{TemplateBinding Foreground}"
                                            TextElement.FontSize="{TemplateBinding FontSize}"
                                            TextElement.FontWeight="{TemplateBinding FontWeight}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Button Name="SuperBTN" Style="{StaticResource myButton}" Grid.Column="1" Grid.Row="1"  
                Content="     Выполнить проверку подключения     " FontSize="16" Foreground="White" 
                FontWeight="DemiBold" Click="SuperBTN_Click" HorizontalAlignment="Center" 
                VerticalAlignment="Center" Margin="10"/>

        <Ellipse Grid.Column="2" Height="100" Width="100" Fill="Orange"/>
    </Grid>
</Window>